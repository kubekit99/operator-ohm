apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openstacklcm.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "openstacklcm.name" . }}
    helm.sh/chart: {{ include "openstacklcm.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "openstacklcm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
