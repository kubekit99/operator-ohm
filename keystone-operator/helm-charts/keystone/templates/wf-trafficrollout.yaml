{{- if eq .Values.oslc.stage "trafficrollout" }}
{{- $envAll := . }}

---
apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  name: {{ printf "%s-%s-%s" $envAll.Release.Name "trafficrollout" "stage" | quote }}
spec:
  entrypoint: whalesay
  templates:
  - name: whalesay
    container:
      image: docker/whalesay:latest
      command: [cowsay]
      args: [{{ printf "%s %s-%s-%s" "Executing trafficrollout:" $envAll.Release.Name "trafficrollout" "stage" | quote }}]
{{ end }}
