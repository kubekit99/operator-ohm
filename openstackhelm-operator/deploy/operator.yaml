apiVersion: apps/v1
kind: Deployment
metadata:
  name: openstackhelm-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: openstackhelm-operator
  template:
    metadata:
      labels:
        name: openstackhelm-operator
    spec:
      serviceAccountName: openstackhelm-operator
      containers:
        - name: openstackhelm-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          ports:
          - containerPort: 60000
            name: metrics
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "openstackhelm-operator"
